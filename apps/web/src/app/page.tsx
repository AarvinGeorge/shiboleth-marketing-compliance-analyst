// meta: U2 dashboard route (07 §4). M0: placeholder proving the shell builds;
// hero metric cards + product cards land at M5 per DESIGN.md.
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-muted-foreground">
        Shiboleth dashboard scaffolded. Surfaces land at M5.
      </p>
    </main>
  );
}
