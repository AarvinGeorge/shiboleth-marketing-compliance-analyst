# Secrets and machine-local config
.env
.env.*
!.env.example
!deploy/.env.prod.example

# Agent context (project norm: code/CLAUDE.md is gitignored)
CLAUDE.md

# Local-only: planning/context, build evidence, dev tooling, and ops runbooks.
# Kept on disk, never pushed to the public repo (the curated design slice lives
# in docs/design/ instead). See docs/design/README.md.
/project-context/
/.progress/
/docs/superpowers/
/tools/
/DEPLOY.md
/OPERATIONS.md

# Local-only living reference + design notes (system understanding, unbuilt
# design proposals). Kept on disk for reference; not shipped to the public repo.
/notes/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/

# Playwright / crawl4ai browser caches
ms-playwright/
.crawl4ai/

# OS
.DS_Store

# Next.js
.next/
out/
*.tsbuildinfo
next-env.d.ts

# lane worktrees (subagent branches; merged via review, never committed here)
.worktrees/
.vercel
